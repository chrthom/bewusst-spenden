import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'bewusstspenden',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
